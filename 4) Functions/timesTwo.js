var x = prompt("Enter a Number: ");
//converts the answer which is a string to a number
var num = parseInt(x, 10)

function timesTwo(x) {
    return 2 * x;    //The function returns the product of x times 2
 }
 
 alert(timesTwo(num));
 
